#!/bin/bash

if [[ " $@ " =~ " COMMON " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "COMMON PACKAGES INSTALL"
  yay -Suy - < ./packages/common
fi

if [[ " $@ " =~ " DOCKER " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "DOCKER INSTALL"
  yay -Suy docker
  sudo groupadd docker
  sudo usermod -aG docker $USER
  newgrp docker
fi

if [[ " $@ " =~ " MINIKUBE " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "MINIKUBE INSTALL"
  curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-latest.x86_64.rpm
  sudo rpm -Uvh minikube-latest.x86_64.rpm
  rm minikube-latest.x86_64.rpm
fi

if [[ " $@ " =~ " PYTHON " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "PYTHON+PYENV INSTALL"
  yay -Suy python python-pip
  curl https://pyenv.run | bash
fi


if [[ " $@ " =~ " NODE " ]] || [[ " $@ " =~ " ALL " ]]; then
  yay -Suy nodejs npm
fi

if [[ " $@ " =~ " TMUX " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "TMUX CONFIG"

  git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

fi
if [[ " $@ " =~ " JETBRAINS " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "JETRBAINS TOOLBOX AND IDEAVIM"
  wget https://download.jetbrains.com/toolbox/jetbrains-toolbox-2.5.2.35332.tar.gz -P /tmp/
  tar -xzf /tmp/jetbrains-toolbox-2.5.2.35332.tar.gz -C /tmp
  mv /tmp/**/jetbrains-toolbox ~/Toolbox
  ~/Toolbox/jetbrains-toolbox
  stow -R ideavim
fi

if [[ " $@ " =~ " YAY " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "YAY INSTALL"
  sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si
fi

if [[ " $@ " =~ " KDE " ]]; then
  echo "KONSAVE IMPORT"
  yay -Suy konsave
  konsave -i ./thinkpad-a.knsv
  konsave -a thinkpad-a
fi

if [[ " $@ " =~ " SWAY " ]]; then
  echo "SWAY SETUP"
  yay -Syu - < ./packages/sway

  stow -S sway waybar scripts zsh2 tmux kickstart-nvim fuzzel mako
fi

if [[ " $@ " =~ " DEVOPS " ]] || [[ " $@ " =~ " ALL " ]]; then
  echo "DEVOPS SETUP"
  yay -Syu - < ./packages/devops

  stow -S k9s
fi

echo "DONE"
